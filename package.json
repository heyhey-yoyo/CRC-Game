{
  "name": "crc-immune-frontier",
  "version": "0.7.0",
  "private": true,
  "description": "CRC Immune Frontier v0.7.0 Public Preview — a science-bounded static educational strategy game",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "check": "node --check js/content-loader.js && node --check js/sim-engine.js && node --check js/sim-worker.js && node --check js/storage.js && node --check js/app.js && node --check sw.js && node --check scripts/build.mjs && node --check scripts/validate-content.mjs",
    "validate:content": "node scripts/validate-content.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test --config playwright.config.mjs",
    "test:browser:install": "playwright install chromium",
    "build": "node scripts/build.mjs",
    "release:check": "npm run check && npm run validate:content && npm test && npm run build && npm run test:browser"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
